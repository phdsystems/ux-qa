# Getting Started

- POST `/api/runs` after each CI job.
- Subscribe to `/api/events` (SSE) for live updates.
- Configure env vars (`PLAYWRIGHT_HUB_STORAGE`, `PLAYWRIGHT_HUB_TELEMETRY`) before booting the hub.
